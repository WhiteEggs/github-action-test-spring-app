# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
      
    - name: Upload artifact
      uses: actions/upload-artifact@v4.6.2
      with:
        name: myapp
        path: target/github-action-test-spring-app.war
        
    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    # - name: Update dependency graph
    #   uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
  deploy:
    runs-on: tomcat-server
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v5.0.0
        with:
          name: myapp
          path: ./deploy
        
      - name: Stop Tomcat
        run: sudo systemctl stop tomcat10
        
      - name: Deploy new WAR
        run: sudo cp ./deploy/github-action-test-spring-app.war /var/lib/tomcat10/webapps
        
      - name: Start Tomcat
        run: sudo systemctl start tomcat10
        
      # - uses: actions/checkout@v4
      # - name: Copy war to server
      #   run: scp target/myapp.jar user@server:/path/to/deploy/
      # - name: Restart app
      #   run: ssh user@server 'systemctl restart myapp.service'
